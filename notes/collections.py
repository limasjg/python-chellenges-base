# Collections = Sigle "Variable" used to store multiple values
# list = [] ordered and changeble. Duplicates OK
# Set = {} unordered and inmutable , but add/remove ok, NO duplicates
# Tuple = () ordered and unchangeable. Duplicates ok. Faster

fruits = ["apple", "orange", "coconut"]

cars = {"ford", "byd", "fiat"}

colors = ("red", "blue", "green")

#print(fruits, cars, colors)

for color in colors:
    print(color)